apiVersion: v1
kind: Service
metadata:
  name: {{ include "background-backend.fullname" . }}
  labels:
    {{- include "background-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "background-backend.selectorLabels" . | nindent 4 }}

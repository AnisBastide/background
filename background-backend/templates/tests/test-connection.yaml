apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "background-backend.fullname" . }}-test-connection"
  labels:
    {{- include "background-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "background-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
